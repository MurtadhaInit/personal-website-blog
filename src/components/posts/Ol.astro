---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"ol">;

const { class: className, ...attrs } = Astro.props;
---

<ol
  {...attrs}
  class:list={["marker:text-fg-muted list-decimal space-y-2 pl-6", className]}
>
  <slot />
</ol>
