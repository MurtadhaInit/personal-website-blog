---
import Link from "@components/Link.astro";

interface Props {
  prevURL: string | undefined;
  nextURL: string | undefined;
}

const { prevURL, nextURL } = Astro.props as Props;
---

<nav>
  {
    prevURL && (
      <Link
        text="Previous"
        href={prevURL}
        style={"primary"}
        icon={{
          name: "tabler:arrow-big-left-line",
          side: "left",
        }}
      />
    )
  }
  {
    nextURL && (
      <Link
        text="Next"
        href={nextURL}
        style={"primary"}
        icon={{
          name: "tabler:arrow-big-right-line",
          side: "right",
        }}
      />
    )
  }
</nav>
